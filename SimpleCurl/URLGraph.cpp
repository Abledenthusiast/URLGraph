#include <string>
#include <iostream>
#include <vector>
#include <cctype>
#include <map>

#include "SimpleCurl.h"
#include "grab_urls.h"

using namespace std;

class URLGraph
{
private: 
	string color;
	vector<string> parent;
	int distance;
public:
	URLGraph(){}


	breadthSearch(map graph, string origin)
	{
		map<string, vector<string>>;
		vector<map> Q;

	}
};